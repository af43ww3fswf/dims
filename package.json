{
  "name": "sitecore-content-hub-dimensions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode external",
    "build:bundle": "vite build --mode external",
    "build:external": "vite build --mode external",
    "build:spa": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "eslint": "^10.10.0",
    "vite": "^6.0.7"
  }
}
